#include <stdio.h>
#define N 10000

int main()
{
    char str[N], out_str[N];

    
    for (int j = 0; j < 256; j++)
    {
        for (int k = 0; ; k++)
        {
            temp = s[k];
            if (temp == j)
            {
                printf("%c", s[k]);
            }
        }
    }
}